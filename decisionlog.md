# Decision log

Each entry records a project decision, who made it (an AI agent, or a human by name), and the reason. Newest entries at the bottom.

| # | Date | Decision | Made by | Rationale |
|---|---|---|---|---|
| 1 | 2026-09-19 | Rebuild the system rather than patch the previous implementation; disregard components that have better replacements. | Human (Grant Walker, via initial prompt) | The previous attempt produced twisted poses and painful bone mapping; the user explicitly allowed replacing components. |
| 2 | 2026-09-19 | Bone mapping must be automatic for Meshy and Mixamo rigs (and robust for other common rigs). | Human (Grant Walker, via initial prompt) | Core usability goal: drop a model in and start. |
| 3 | 2026-09-19 | Support graceful reduction of animation as fewer landmarks are visible, plus a mirror-style virtual camera that frames the model as the webcam frames the user. | Human (Grant Walker, via initial prompt) | Requested explicitly as alternatives to forcing full-body view. |
| 4 | 2026-09-19 | Provide a calibration path including a side-by-side snapshot that a human or LLM can use to diagnose alignment. | Human (Grant Walker, via initial prompt) | User's idea; implemented as automatic rig analysis + optional pose calibration + diagnostic snapshot bundle. |
| 5 | 2026-09-19 | Support recording: raw landmark takes, retargeted animation as a 3D asset (GLB with model and optional scene), BVH, and viewport video. | Human (Grant Walker, via initial prompt) | Intended uses: interactive art, mocap recording, recording actor takes in a 3D scene. |
| 6 | 2026-09-19 | Develop directly on `main` (the designated branch is also pushed for the session). | Human (Grant Walker, via initial prompt) | Project is early; user asked to keep changes on main. |
| 7 | 2026-09-19 | Keep a decision log (this file). | AI agent (proposed; awaiting user confirmation) | The user's preferences ask to log decisions; the file did not exist, so it was created and flagged for the user to keep or remove. |
| 8 | 2026-09-19 | Run pose tracking in the browser with MediaPipe Tasks Vision (PoseLandmarker; optional Hand/Face landmarkers) instead of a Python/OpenCV backend with a pywebview GUI. | AI agent | Zero-install for users, lower latency (no JSON hop), same landmark quality as the Python package; the old pywebview GUI and video "visual modes" were unrelated to the core goal. |
| 9 | 2026-09-19 | Keep an optional headless Python provider (OpenCV + MediaPipe Tasks) that streams the same PoseFrame v2 protocol over WebSocket. | AI agent | Preserves the OpenCV path the user mentioned for other cameras or multi-machine setups at low cost; no GUI framework. |
| 10 | 2026-09-19 | Retarget with a world-space delta from the bind pose ("world-delta solver") using full 3-axis measured bases from landmark triplets, instead of `setFromUnitVectors(+Y, dir)` on local bone axes. | AI agent | The previous approach assumed every bone's rest axis was local +Y and ignored bind pose, which is the direct cause of the twisted poses. |
| 11 | 2026-09-19 | Use the skin's inverse bind matrices (bind pose) as the rest pose, not the node transforms in the file. | AI agent | The bundled Meshy sample stores a non-rest pose in node transforms; skin weights are painted for the bind pose. |
| 12 | 2026-09-19 | Per-bone reference modes: `auto` (model bind direction), `relative` (canonical human rest), `calibrated` (user-measured), chosen by anatomical plausibility of the rig's bind pose, overridable. | AI agent | The Meshy sample rig has a non-anatomical stub thigh bone; a single global strategy cannot serve both standard and stylized rigs. |
| 13 | 2026-09-19 | Auto-mapping combines a name lexicon with hierarchy/topology analysis; topology decides spine order and side. | AI agent | Meshy names the spine `Spine02 → Spine01 → Spine` from the hips upward (reverse of Mixamo); names alone are unreliable. |
| 14 | 2026-09-19 | Mirror mode on by default; MediaPipe always runs on the un-flipped frame. | AI agent | Mirror behaviour feels natural for interactive use; flipping the frame before inference would corrupt MediaPipe's left/right labels. Actor (non-mirrored) mode is one toggle for performance recording. |
| 15 | 2026-09-19 | Stack: Vite 8, TypeScript 7 (strict), three r186, `@mediapipe/tasks-vision` 1.0.1, optional `@pixiv/three-vrm` 3.5; Vitest unit tests; Playwright end-to-end tests using synthetic recordings. | AI agent | Type checking and tests let the agent validate without a webcam; Playwright's headless Chromium here supports WebGL2 and WASM. |
| 16 | 2026-09-19 | Recording formats: `.mocap.json` (raw PoseFrame v2 stream), GLB with embedded animation (via GLTFExporter), BVH, WebM video. | AI agent | Raw takes are lossless and re-targetable; GLB is the most portable 3D asset; BVH is the DCC standard for mocap; WebM is native to the browser. |
| 17 | 2026-09-19 | Normalize every loaded model to a target height (1.7 m) via root scale; remember the original scale for export. | AI agent | Mixamo FBX exports are in centimeters; unit-consistent scale is needed for hips translation, camera framing and exports. |
| 18 | 2026-09-19 | Remove legacy files: `main.py` (mock), `backend/main.py` and `backend/gui/`, `frontend/`, `architecture.md`, `projectplan.md`. Keep the sample model as `public/models/sample-meshy.glb`. | AI agent | Replaced by the new architecture; the sample rig is a valuable test asset (Meshy quirks). |
| 19 | 2026-09-19 | Add "props and weapons in the character's hands" to the roadmap, built as an alignment tool: automatic first pass, manual slider correction, export of the corrected offsets as new defaults. | Human (Grant Walker) | Requested in chat; the bundled weapon pack (models/weapons) is the first target. |
| 20 | 2026-09-19 | Classify the propose → correct → persist loop as mixed-initiative, human-in-the-loop calibration (direct manipulation over an AI prior, corrections stored as presets) and adopt it as the standard pattern for every alignment problem in the project (bone roll trims, reference modes, pose calibration, prop sockets, camera presets). | AI agent (in response to Grant Walker's question) | One consistent interaction model keeps the UI predictable and makes user corrections reusable data. |
| 21 | 2026-09-19 | Use the new sample pack (28 characters, 29 weapons on branch `models-ingest`) as test fixtures and bundled samples; bind pose from skins and skeleton-based height are mandatory because these files store animation poses in node transforms and a 0.01 root scale. | Human (Grant Walker, provided the models) / AI agent (analysis) | Real-world data for the auto-mapper and rest analysis; one rig has scrambled spine/neck names which validates topology-first mapping. |
| 22 | 2026-09-19 | Adopt design revision 2.1 from the adversarial review: bind pose recovered from inverse bind matrices against the real parent transform (never `Skeleton.pose()`); in-plane, side-independent up references (flexion direction, kneecap direction, dorsal normal, foot forward); auto-mode references derived by running the same estimators on the rig's bind skeleton; torso chain driven in `relative` mode with a standing baseline; swing/twist about the pre-rotation axis with a low-passed twist state; no-knee chains driven as one segment (`follow` mode). | AI agent (review findings verified numerically on the sample rig) | The original §6 conventions contradicted each other (side-signed bend normals vs +Z canonical) and would have reproduced the twisted-arm failure (62° twist on the sample rig); `Skeleton.pose()` double-applies armature transforms. |
| 23 | 2026-09-19 | Auto-mapping uses an extremity-path topology detector (LCA of feet/hands/head) with name classes (torso/arm/leg/finger + side) constraining chain membership; sides come from names when consistent and facing is derived from them; helper bones classified on whole tokens with skin-weight checks; rig identity split into `familyKey` (mapped subgraph) and `instanceKey` (bind signature); profiles persist user intent only. | AI agent (review findings) | Child-count rules failed on Genesis/CC4/Rigify/UE5 and on the sample's stub thigh; a single hierarchy hash collided across all rigs of a family while breaking on trivial re-exports. |
| 24 | 2026-09-19 | Mirror camera framing from a continuous least-squares fit of image y against user body proportions (with framing states full/waist/bust/face/none and hysteresis); hips depth from world-vs-image segment lengths with an assumed 45° webcam FOV; gating with dwell, in-frame test and per-group thresholds; scale-free One Euro (beta 30); explicit hips-lost policy for seated users. | AI agent (review findings) | Bounding-landmark framing jumped at every gate flip; torso-size depth was foreshortening-sensitive; pixel-tuned One Euro never adapted on normalized coordinates; seated users would rock from hallucinated hip landmarks. |
| 25 | 2026-09-19 | Export details: BVH writes world deltas re-expressed in the mapped parent's delta frame (ZXY, End Sites, absolute root); GLB export poses the rig to bind first, exports the wrapper group with uuid-named tracks; video composites into an even-sized canvas with a codec probe and WebM duration patch; takes carry camera/tracker/calibration metadata and raw frames. | AI agent (review findings verified against three r186 sources) | GLTFExporter serializes live node transforms and resolves tracks by name; BVH has no rest rotations; Chrome WebM files lack a duration. |
| 26 | 2026-09-20 | Ship the session's state on `main`: build, 313 unit tests and the six-scenario Playwright suite pass; reviewer findings for the solver and diagnostics that could not be applied in-session are recorded in `docs/REVIEW-NOTES.md` for the next pass. | Human (Grant Walker: "wrap it up") / AI agent | Deliver a working, verified baseline now; the remaining findings are refinements, and live-webcam validation with the diagnostic snapshot is the next step. |
| 27 | 2026-09-20 | Control panels update the DOM in place on the 100 ms refresh: `select.set`/`setOptions` are no-ops when nothing changed, the bone-mapping table is rebuilt only when its structure changes (error cells update in place), and the WebSocket URL field is never overwritten while focused. The dev server serves the MediaPipe bundle without its broken `sourceMappingURL` comment. | AI agent (in response to Grant Walker's bug report) | The periodic refresh recreated every dropdown's options, which closes an open dropdown at once (the camera selector "jumping") and stalled the page with DOM churn; the "Failed to load source map" message is an upstream packaging typo in `@mediapipe/tasks-vision` 1.0.1 and only looked like the startup error. |
